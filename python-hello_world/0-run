#!/bin/bash
# This script runs a Python script whose name is stored in the environment variable $PYFILE

# Check if the PYFILE variable is set
if [ -z "$PYFILE" ]; then
    echo "Error: PYFILE environment variable is not set."
    exit 1
fi

# Run the Python script
python3 "$PYFILE"
